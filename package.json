{
	"name": "json2const",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"check": "biome check --write",
		"check:ci": "biome check",
		"format": "biome format --write",
		"format:ci": "biome format",
		"typecheck": "tsc --noEmit",
		"verify": "bun run check:ci && bun run format:ci && bun run typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"jsonc-parser": "^3.3.1"
	}
}
